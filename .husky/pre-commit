# TakumiDeck Pre-Commit-Gate (Working Rule 6):
# typecheck, lint und Tests müssen grün sein, sonst wird der Commit abgebrochen.
# Lint strikt: jede Warnung wird als Fehler behandelt (--max-warnings=0).
npm run typecheck && npm run lint -- --max-warnings=0 && npm test
